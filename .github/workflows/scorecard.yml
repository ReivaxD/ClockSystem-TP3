

name: scorecard

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read


    env:
      DISPLAY: ":99.0" # Set a display for Xvfb

    services:
      xvfb:
        image: selenium/standalone-chrome:latest
        options: -e VNC_SERVER=true -e VNC_PORT=5900

    steps:
    - name: "Run analysis"
      uses: ossf/scorecard-action@<VERSION OR BRNACH OR TAG OR SHA>
      with:
        results_file: results.sarif
        results_format: sarif
        publish_results: true

    - name: "Upload artifact"
      uses: actions/upload-artifact@<VERSION OR BRNACH OR TAG OR SHA>
      with:
        name: SARIF file
        path: results.sarif
        retention-days: 5

    - name: "Upload SARIF results to code scanning"
      uses: github/codeql-action/upload-sarif@<VERSION OR BRNACH OR TAG OR SHA>
      with:
        sarif_file: results.sarif

        


